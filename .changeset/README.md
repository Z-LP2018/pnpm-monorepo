# Changesets

此目录用于管理项目版本和变更日志。

## 使用

### 创建变更集

```bash
pnpm changeset
```

这将引导您创建一个变更集，描述您的更改。

### 版本发布

```bash
pnpm version
```

这将根据变更集更新版本号并生成变更日志。

### 发布

```bash
pnpm release
```

这将构建项目并发布到 npm。

## 更多信息

查看 [Changesets 文档](https://github.com/changesets/changesets) 了解更多。
